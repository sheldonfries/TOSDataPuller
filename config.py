# Directory where TOSDataBridge/bin is located
dir = "C:/TOSDataBridge/bin"

# Symbols which data will be pulled for (currently only supports the 3; up to n in the future)
items = ['SPY', 'GOOG', 'AAPL']

# Topics which dictate the data to be pulled for each symbol (currently only supports the 3; up to n in the future)
topics_for_items = ['LAST', 'VOLUME', 'CUSTOM1']